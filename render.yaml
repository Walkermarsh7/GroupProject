services:
  - type: web
    name: my-dash-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      PYTHON_VERSION: 3.11.9
